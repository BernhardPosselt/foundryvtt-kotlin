<div>
    <div class="row">
        <table>
            <thead>
            <tr>
                <th>Name</th>
                {{#each additionalColumnHeadings}}
                    <th>{{this}}</th>
                {{/each}}
                <th>Enable</th>
                <th>Edit</th>
                <th>Delete</th>
            </tr>
            </thead>
            <tbody>
            {{#each items}}
                <tr>
                    <td>{{name}}</td>
                    {{#each additionalColumns}}
                        <td>{{this}}</td>
                    {{/each}}
                    <td>{{> formElement enable }}</td>
                    <td>
                        <button
                                type="button"
                                data-action="edit"
                                data-id="{{id}}"
                                title="Edit"
                                {{#unless canBeEdited}}hidden{{/unless}}
                        ><i class="fa-solid fa-pencil"></i></button>
                    </td>
                    <td>
                        <button
                                type="button"
                                data-action="delete"
                                data-id="{{id}}"
                                title="Delete"
                                {{#unless canBeDeleted}}hidden{{/unless}}
                        ><i class="fa-solid fa-minus"></i></button>
                    </td>
                </tr>
            {{/each}}
            </tbody>
        </table>
    </div>
    <button type="submit" class="full-width" data-action="save">Save</button>
</div>